test_format = 1.0

[default]

	# ------------
	# Tests to run
	# ------------
	preinstall = """
	sudo yunohost tools update apps
	sudo yunohost domain add influxdb.domain.tld
	sudo yunohost app install https://github.com/YunoHost-Apps/influxdb_v2_ynh/ \
	--args "&domain=influxdb.domain.tld&init_main_permission=visitors&admin=package_checker&password=p@ssw0rd&influxdb_org_name=flux-org&influxdb_bucket_name=flux-bucket&influxdb_bucket_retention=30d&is_public=1&path=/&" \
	--force
	"""

	# -------------------------------
	# Default args to use for install
	# -------------------------------

	# -------------------------------
	# Commits to test upgrade from
	# -------------------------------
	test_upgrade_from.552b0e0e1fd1b3cf0d447391d9dc0717d0887f5c.name = "Upgrade from 0.8.6~ynh1"
    
